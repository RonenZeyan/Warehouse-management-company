class Solution(object):
    def convert(self, s, numRows):
        """
        :type s: str
        :type numRows: int
        :rtype: str
        """

        strLength = len(s)
        matrix = [numRows][]
        i=0 
        j=0
        for ch in s:
            if i<numRows-1:
                matrix[i][j]=ch
            else:
                i=
                matrix[i]

            
        